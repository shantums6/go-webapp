apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-webapp-chart.fullname" . }}
  labels:
    {{- include "go-webapp-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-webapp-chart.selectorLabels" . | nindent 4 }}
